server:
  port: ${PORT:8080}
#jpa:
#  hibernate:
#    dialect-class-name: org.hibernate.dialect.PostgreSQLDialect
#    show_sql: true
#db:
#  username: ${USERNAME:postgres}
#  password: ${PASSWORD:toor}
#  pool.size: ${POOL_SIZE:20}
#  driver-class-name: org.postgresql.Driver
#  url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DATABASE:rent_service}
spring:
  application:
    name: simple-web-app
  jpa:
    show_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    username: postgres
    password: toor
    pool.size: 20
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/rent_service
